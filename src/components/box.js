import React from "react";

const Box = ({children}) => (

        <div className="bg-white border-border-grey shadow-shadow-grey drop-shadow-lg font-poppins font-normal box-border border-2 rounded-[20px] w-full aspect-square flex content-center m-auto">
                {children}
        </div>

)

export default Box